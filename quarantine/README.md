# Quarantine

Files moved here are no longer part of the active build or authoring pipeline. They are kept for reference only.

## hosting/
Cloudflare Pages / Docker / Nginx deployment infrastructure from when the game was hosted online. The game is now local-only (`launch.bat` → `dist/index.html`).

- `Dockerfile`, `docker-compose.yml`, `nginx.conf` — container/server deployment
- `_headers`, `_redirects` — Cloudflare CDN configuration
- `deploy.yml` — GitHub Actions workflow that pushed to Cloudflare Pages on every commit (disabled by moving out of `.github/workflows/`)

## stale-docs/
Documentation that describes processes or states that no longer apply.

- `FIXES_COMPLETE.md` — batch-era fix log, superseded by git history
- `RELEASE.md` — hosted release process, no longer relevant
- `ITCH_IO_LISTING.md` — itch.io storefront copy, game no longer listed there

## itch-io/
itch.io build artifacts from the prior hosting period. Not generated by the current `build.py`.
