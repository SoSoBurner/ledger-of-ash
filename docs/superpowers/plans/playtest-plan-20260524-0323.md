# Playtest Fix Plan — 20260524-0323
**Source:** playtest-report-20260524-1045-headed.md
**Generated:** 2026-05-24T10:23:37.243Z

## P0 — Critical (fix before any build)
- Engine / Logic Bugs: [P0] Dead-ends Log Empty Snippets** — All 13 dead-ends in the log show blank HTML ` ` instead of narrative content (picks 104, 134, 164, 190, 220, 95, 125, 155, 185, 215, 245, 275, 329). This blocks manual audit of what text is actually being served at dead-end stalls. Root: narrative/voice audits incomplete (from daily log), or `loadChoices`/enriched-choice logic returns undefined narrative on dead-end redirect.
- Engine / Logic Bugs: [P0] Stall-timeout in classic-combat/warrior** — Family timed out at 220 picks. Symptom: shelkopolis ↔ cosmoria loop with 6 dead-ends in shelkopolis alone. Daily log suspects `stage2_enriched_choices.js` or `travel_corridors.js` returning empty/undefined, causing engine to retry choice load indefinitely. Likely: `getEnrichedChoices()` or `getTravelChoices()` returns `[]` or `undefined` for certain locality/state combos, triggering infinite `loadChoices` retry.
- Stage Progression / sp2 Flow: [P0] stage2_enriched_choices.js or travel_corridors.js not injecting sp2 rewards on Stage II localities** — Cosmoria visited 18 times with 0 sp2 contribution. Shelkopolis visited 23 times with only 1 sp2 contributed. The sp2-advancing enriched choice pool is either missing entirely from travel destinations, or is gated by an uninitialized flag. Immediate root-cause: check `stage2_enriched_choices.js` to verify it's being called on cosmoria/fairhaven/soreheim arrivals, and check that `G.flags.stage2_enriched_choices_enabled` (or equivalent gate) is being set correctly before first Stage II travel.
- Stage Progression / sp2 Flow: [P0] Dead-end log contains 13 entries with empty HTML snippets (blank narrative text)** — Examples: picks 104, 134, 164, 190, 220, 95, 125, 155, 185, 215, 245, 275, 329 all render as whitespace-only. This is a silent narrative failure—the choice fired but the result text payload is missing. Check `post_run_analysis.js` to confirm it's capturing HTML correctly, and verify `narrative_lookup.js` has entries for all Stage II localities/routes being visited.
- Content / Narrative Quality: [P0] Dead-ends with empty result narrative (13 occurrences)**
- Combat / Balance: [P0] Stage II choice routing — infinite loops in warrior/garrison and rogue/shelk families**
- Locality Coverage: [P0] sp2-enriched choices not firing in Stage II localities** — Six localities visited but contributed 0 sp2: cosmoria (18 visits), fairhaven, soreheim, ithtananalor, aurora, sunspire. This stalls progression entirely. Root: `stage2_enriched_choices.js` filtering or trigger logic failing for these locales. These 6 must fire to advance Stage II.
- Locality Coverage: [P0] Dead-end HTML snippets empty** — All 13 dead-ends logged with blank HTML (`" "`). Narrative text missing. Dead-end rendering broken — either `narrative_lookup.js` keys missing or snippet assembly failing. Blocks narrative audit.
- Polish / UX: [P0] Dead-end narratives render with blank HTML snippets**
- Voice and Register Audit: [P0] Dead-ends log — empty HTML snippets prevent voice audit**
- Branch Drift Audit: [P0] Dead-end narratives rendering blank**
- HUD / G-State Cross-Reference: [P0]** [harness — screenshot capture] — **0 screenshots captured across 1126 picks and 6 families.** Every visual HUD field in my domain (HP, XP, level, gold, stage label, locality name, sp1/sp2 progress bars, skill rows, faction heat, alignment bars) is unverifiable. The entire HUD-vs-log cross-reference audit is blocked at the data-collection layer. The report header explicitly lists this: `screenshots captured` = 0.
- HUD / G-State Cross-Reference: [P0]** [harness — HUD mismatch probe] — **`## HUD MISMATCHES` log section is empty (0 entries)** despite 1126 picks across 6 families with multiple stage transitions and sp2 thresholds crossed. Two possible readings: (a) every HUD field matched G-state for the entire run, or (b) the `[hud-integrity]` / `[hud-mismatch]` probe never fired. Combined with 0 screenshots and the empty `## DOM DUPLICATES` section, reading (b) is more plausible — the HUD audit instrumentation appears to not be running. Cannot confirm clean run without independent evidence.
- HUD / G-State Cross-Reference: [P0] #2 above it is more likely a probe-fired-zero-times artifact than a true clean signal.
- UI Duplication / Double-Render Audit: [P0]** [harness — DOM DUPLICATES section] — The `## DOM DUPLICATES` log section is empty across all 1126 picks. Either the duplicate-probe never fired during this headed run, or it fired and detected nothing but didn't emit a "0 duplicates checked" sentinel line. Without a positive "probe ran, found nothing" signal, an empty section is indistinguishable from probe failure — and a probe that didn't run cannot certify the singleton invariants (`#hud-hp`, `.result-text`, `.stage-banner`, `.levelup-notice`, `.env-desc`).
- UI Duplication / Double-Render Audit: [P0]** [harness — screenshots] — 0 screenshots captured across 6 family runs and 1126 picks. Visual audit of doubled stat values, stacked button labels, or visibly duplicated UI elements is impossible without artifacts. This blocks the visual half of the duplication audit entirely.

## P1 — Important (fix in next session)
- Engine / Logic Bugs: [P1] Same-to-same Travel Loop** — Travel Log shows 11 shelkopolis→shelkopolis, 8 cosmoria→cosmoria, 2 soreheim→soreheim out of 38 total travels (29% confirmed same-to-same visible; daily log notes 62% ratio overall). Root: `travel_corridors.js` `getTravelChoices()` not filtering out current locality, or `stage2_travel` logic missing destination validation. Travel should exclude current locality from destination pool.
- Engine / Logic Bugs: [P1] Coverage Gaps (6 uncontributing localities)** — cosmoria, fairhaven, soreheim, ithtananalor, aurora, sunspire each visited but with 0 sp2 contribution. Possible causes: (a) no sp2-advancing choices exist for these localities in enriched pool, (b) sp2 choices exist but gate on `G.flags.sp2_unlocked` and weren't triggered, or (c) `stage2_enriched_choices.js` filters exclude these localities. Verify: inspect enriched choice pool for these 6 localities; check if conditions gate sp2 choices.
- Engine / Logic Bugs: [P1] Max-picks in rogue/stealth-precision** — Family hit 350 picks before timeout (choice limit). Rogue ended at sp2=129 (vs 18 expected at cap). Suggests: sp2 cap gate (`if (G.sp2 >= 12)`) either not firing or being bypassed. Verify: `updateHUD()` sp2 display vs actual `G.sp2` value; check if enriched choices are modifying sp2 post-cap.
- Stage Progression / sp2 Flow: [P1] Six Stage II localities show 0 sp2 contribution despite visitation** — cosmoria (18 visits), fairhaven (1), soreheim (2), ithtananalor (1), aurora (1), sunspire (1). Coverage gap: either these localities have no enriched-choice injection wired, or the choices in the base locality pools are missing `{type:'sp2', n:...}` effects entirely. Verify `stage2_enriched_choices.js` has entries for all 6, and that arrival hooks on each locality are calling `stage2_enriched_choices.js` before rendering choices.
- Stage Progression / sp2 Flow: [P1] Two families stalled at abnormally high sp2 — warrior at 86, rogue at 129 — well above Stage II cap of 18** — Both hit hard stops (stall-timeout, max-picks). Suggests they cycled within a single locality or locality pair rather than advancing toward Stage III gate. Inspect travel routes: check `travel_corridors.js` to see if it's returning non-empty destination pools, and check that `resolveArrival()` is advancing locality before re-rolling choices.
- Stage Progression / sp2 Flow: [P1] Map travel shows heavy self-loops** — shelkopolis→shelkopolis 17 times, cosmoria→cosmoria 8 times. Travel destinations may be collapsing to current locality, or route selection is narrowing incorrectly. Check `travel_corridors.js` return signature: confirm it's returning an array of valid destination keys, not `undefined` or the current locality by default.
- Content / Narrative Quality: [P1] Stage II localities with zero sp2 contribution despite visits**
- Combat / Balance: [P1] Zero sp2 contributors in 6+ Stage II localities**
- Locality Coverage: [P1] Map travel self-loops dominate** — 20+ travel logs show same→same (shelkopolis→shelkopolis ×12, cosmoria→cosmoria ×8, fairhaven→fairhaven ×2, etc.). Travel corridors not selecting different destinations. `travel_corridors.js` likely returning current locale instead of advancing.
- Locality Coverage: [P1] Stall-timeout in classic-combat/warrior** — Family hit timeout at pick 220 (stage ceiling not breached). Suggests encounter cycling loop in that archetype+background combination (warrior + w_garrison).
- Locality Coverage: [P1] Locality coverage collapse** — 24 unvisited localities (only 8 of 32 visited). Stage II travel routing too narrow; most content unreachable. Roaz, Sheresh, Cysur, Nomdara, Whitebridge, and other major hubs never entered.
- Polish / UX: [P1] No screenshot captures from headed spec run**
- Branch Drift Audit: [P1] Six localities with zero sp2 contribution despite visits**
- Branch Drift Audit: [P1] Extensive self-loop travel pattern**
- HUD / G-State Cross-Reference: [P1]** [report — sp2 column vs HUD] — Final sp2 values logged in the report header per family (86, 18, 129, 18, 18, 18) are the only HUD-adjacent G-state values exposed in this report. The four successful runs all terminate at exactly `sp2=18` (Stage II antechamber/climax threshold). No screenshot exists to verify the sp2 progress bar visually showed `18/18` (or whatever the UI maximum is) at end-of-run. Cannot validate sp2 HUD rendering against the logged value.
- UI Duplication / Double-Render Audit: [P1]** [harness — dead-ends log] — All 13 dead-end entries have empty HTML snippets (` `). Dead-end HTML is exactly where duplicate `.result-text` rendering and stacked choice labels would show up. With empty snippets, I cannot scan for duplicate choice labels or repeated result-text blocks at the moments most likely to expose them. Snippet capture for dead-ends needs to be fixed before this audit dimension can return meaningful results.

## P2 — Nice to have (backlog)
- Polish / UX: [P2] Self-looping travel corridors create navigation dead zones**
- HUD / G-State Cross-Reference: [P2]** [report — observable consistency] — No `[hud-integrity] VIOLATION` entries, no `## JS ERRORS` (0), and `## DOM DUPLICATES` empty. Taken at face value this is clean, but per [P0] #2 above it is more likely a probe-fired-zero-times artifact than a true clean signal.

---
*Generated by post-run-analysis.js from playtest-report-20260524-1045-headed.md*