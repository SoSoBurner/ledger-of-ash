# Playtest Fix Plan — 20260611-1955
**Source:** playtest-report-20260611-1613-headed.md
**Generated:** 2026-06-12T02:55:32.028Z

## P0 — Critical (fix before any build)
- Engine / Logic Bugs: [P0] Abilities Never Unlock Across All Archetypes** — All four families report `abilities=0` regardless of archetype (paladin, oracle, rogue, healer). This is a 4/4 failure pattern. The screenshot list includes ability-related captures (`ability_card_p2.png`, `active_ability_use.png`), so the UI code exists, but `G.unlockedCombatAbilities` is never populated. This is likely either:
- Engine / Logic Bugs: [P0] Skill Routing Broken — All Rolls Default to Might** — Every family shows `dominant=might(2+)` with finesse/vigor/wits/charm/spirit all = 0. Across 564 picks and four completely different archetypes, this is impossible if choices specify varied skills. Either:
- Engine / Logic Bugs: [P0] Four Localities with Zero sp2 Contribution Despite Multiple Visits** — cosmoria (43 visits), mimolot (3 visits), shirshal (1 visit), aurora (3 visits) all visited but never advanced stage progress. This suggests:
- Stage Progression / sp2 Flow: [P0] cosmoria — 43 visits, 0 sp2 contribution**
- Combat / Balance: [P0] `ledger-of-ash.html` — All archetypes report abilities=0 across 564 picks**
- Combat / Balance: [P0] `content/` — Extreme heat imbalance between families**
- Locality Coverage: [P0] cosmoria — 43 visits, zero sp2 contribution**
- Polish / UX: [P0] Abilities Count Display — All families show `abilities=0`**
- HUD / G-State Cross-Reference: [P0] [archetype signatures — abilities=0 across all 4 families]** — `paladin abilities=0`, `oracle abilities=0`, `rogue abilities=0`, `healer abilities=0`. Per memory `[feedback_abilities_ui_location]` and `[feedback_harness_metrics_diagnosis]`, the harness reports `abilities` as a DOM button text count (combat UI / sheet), not `G.unlockedCombatAbilities`. Zero across all four families — including paladin (160 picks, lvl-up×3) and oracle (341 picks, lvl-up×5) — is a HUD/state divergence: abilities are seeded into G at archetype select but never surfaced into the combat UI / character sheet that the harness scans. This is the known ability-effects/dispatcher gap recorded in `now.md`.

## P1 — Important (fix in next session)
- Engine / Logic Bugs: [P1] Rogue Soft Timeout at Pick 50** — stealth-precision family hit a timeout with only sp2=7 (much lower than paladin's 160 picks, oracle's 341 picks). "Soft" suggests a Playwright timeout, not a crash:
- Engine / Logic Bugs: [P1] Healer Page-Closed at Pick 11** — Extremely early failure with sp2=0 (never leaves Stage 1). Either:
- Engine / Logic Bugs: [P1] Energy Economy Not Instrumented** — The report includes heat metrics but no gold/supply tracking. From memory (feedback_harness_metrics_diagnosis.md): "Trace any playtest report metric to its harness source before diagnosing a bug." The P0 economy issues from earlier analysis (zero gold income, 4x start-gold variance) cannot be verified without harness instrumentation. The missing telemetry makes it impossible to confirm whether the economy bugs are real or whether choice effects are being applied.
- Stage Progression / sp2 Flow: [P1] Mimolot, shirshal, aurora — zero sp2 across 7 combined visits**
- Stage Progression / sp2 Flow: [P1] sp2 concentrated in two localities**
- Stage Progression / sp2 Flow: [P1] Stealth-precision timeout at pick 50 (sp2=7)**
- Content / Narrative Quality: [P1] Stage II localities lacking sp2-advancing content:**
- Combat / Balance: [P1] Enriched choice pool — Skill diversity collapse (might-only dominance)**
- Combat / Balance: [P1] `content/` — Rogue soft-timeout at pick 50**
- Combat / Balance: [P1] `content/` — Healer early termination at pick 11**
- Combat / Balance: [P1] `content/` — XP/level pacing variance**
- Locality Coverage: [P1] sp2 pool concentration — 4 localities visited with zero advancement**
- Locality Coverage: [P1] Map travel is shelkopolis-centric**
- Polish / UX: [P1] Font Rendering Issues**
- Polish / UX: [P1] Travel Density/Layout**
- Tutorial and Onboarding Review: [P1] Early Exit — Support/Leadership (Healer) Family**
- HUD / G-State Cross-Reference: [P1] [stage label vs G.stage at Stage II climax]** — Oracle reached `Stage II` with `final sp2 = 24`, paladin reached `Stage II` with `sp2 = 13`. Healer ended at `Stage I` with `sp2 = 0` after only 11 picks (page-closed). No HUD-stage diff captured, but the absence of a `checkStageAdvance` violation log when healer crashed at pick 11 cannot be verified — recommend the next run dump `G.stage` + HUD `.stage-label` at each pick to confirm parity, especially on transition picks.
- HUD / G-State Cross-Reference: [P1] [faction heat HUD verification — shelk=10 in 2 families]** — Both classic-combat (paladin) and magic-spellcasting (oracle) hit `shelk=10` (cap). No screenshots of heat HUD at cap are confirmed in the listed thumbnails (only `_p20_sp9.png` etc. — no `_heat_cap_shelk.png`). Cannot verify whether the HUD shows the saturated value or rolls over. Add a heat-cap screenshot trigger when any polity hits 10.

## P2 — Nice to have (backlog)
- Combat / Balance: [P2] `data/` — Four localities visited with zero stage-progression contribution**
- Locality Coverage: [P2] Stage 2 localities severely under-traveled**
- Locality Coverage: [P2] No dead-ends reported**
- Polish / UX: [P2] Raw ID Display Leak — travel_corridors.js**
- Tutorial and Onboarding Review: [P2] Skill Distribution Imbalance**
- HUD / G-State Cross-Reference: [P2] [skill balance matrix all-zero non-might columns]** — Every family shows `might>=1`, all other skills `0` in usage. Either (a) harness counts only `_lastRollInfo.skill === 'might'` after normalization (engine maps combat→might via `_KEY_NORM`), and other skills genuinely weren't rolled, or (b) HUD displays under display-name keys while harness reads internal keys. The rogue showing might=2/finesse=0 is implausible for a stealth-precision family and suggests a normalization mismatch between the harness probe and `G.skills`. Verify by logging raw `G.skills` object alongside the matrix.
- HUD / G-State Cross-Reference: [P2] [coverage gaps not a HUD issue but flags location-name field]** — `cosmoria` 43 visits / 0 sp2, `shirshal`, `aurora`, `mimolot` 0 sp2. Confirms the harness IS tracking `G.location` against pick events; no HUD location-name mismatches reported. Treat as evidence the location-name HUD field is rendering correctly when it advances.
- HUD / G-State Cross-Reference: [P2] [no `[hud-integrity]` instrumentation appears to be in the harness]** — The empty `HUD MISMATCHES` section across 564 picks, 408 screenshots, and 4 families is suspicious. A run that exercises Stage I→II transition (paladin & oracle) and triggers heat caps (shelk=10) should produce at least *some* HUD/G-state edge cases worth logging. Recommend adding `assertHudMatchesG()` instrumentation that diffs `.hud-hp`, `.hud-xp`, `.hud-level`, `.hud-gold`, `.stage-label`, `.location-name`, `.sp1-bar`, `.sp2-bar`, each `.skill-row`, each `.heat-row`, and `.alignment-bar` against G on every pick — emit `[hud-mismatch]` when divergent.

---
*Generated by post-run-analysis.js from playtest-report-20260611-1613-headed.md*