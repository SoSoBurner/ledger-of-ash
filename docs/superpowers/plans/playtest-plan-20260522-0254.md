# Playtest Fix Plan — 20260522-0254
**Source:** playtest-report-20260522-0215-headed.md
**Generated:** 2026-05-22T00:54:33.654Z

## P0 — Critical (fix before any build)
- Locality Coverage: [P0] Cosmoria lacks sp2-advancing choices** — Visited 8 times (first arrival at pick 32), contributed 0 sp2. Report explicitly flags: "visited but no sp2-advancing choices triggered." Players can reach cosmoria but find no path to progress. Aligns with backlog item "cosmoria journal categories" (P0).

## P1 — Important (fix in next session)
- Stage Progression / sp2 Flow: [P1] Cosmoria sp2 Contribution Gap** — Cosmoria was visited 8 times but contributed 0 sp2, despite being frequented in the early-mid game (first visit at pick 32). Report explicitly flags this as a coverage gap. Either:
- Locality Coverage: [P1] Map travel destinations become dead-ends** — Four locations (fairhaven, guildheart, panim, ithtananalor) appear as travel *destinations* in the log but never as *origins* for subsequent travels. After the harness arrives at these, it either cannot travel onward or no travel options present. This traps progression in isolated pockets of the map.
- Locality Coverage: [P1] Extreme locality bottleneck vs. Stage II scope** — Only 3/28+ localities visited across 4 families, 449 picks, and 10 map travels. Per CLAUDE.md: Stage II is "Completable end-to-end" and must "EXCEED Stage 1 total content volume." Current coverage (~1% of the map) is far below expansion target. Either travel is too rare, travel options too narrow, or localities lack choices that present travel.
- Branch Drift Audit: [P1] cosmoria — Zero sp2-advancing choices contributed**
- HUD / G-State Cross-Reference: [P1] [harness/screenshot capture] — **0 screenshots captured** across 449 picks. Visual HUD verification (blank fields, cut-off values, layout regressions, faction heat row rendering, alignment bars at threshold) is impossible from log data alone. The headed harness's HUD-integrity probe relies on DOM-vs-G comparison, which catches value mismatches but not visual rendering bugs (clipped text, overlapping bars, missing badges). Recommend confirming the screenshot probe is wired and writing to the expected `tests/` path before next playtest.

## P2 — Nice to have (backlog)
- Content / Narrative Quality: [P2] cosmoria locality** — Visited 8 times across runs but **contributed 0 sp2 advancement**. This suggests either:
- Locality Coverage: [P2] Travel trigger frequency may be low** — Only 10 map travels across 449 picks (~1 per 45 picks). May be by design; verify intended travel frequency if travel system is active in this stage.
- HUD / G-State Cross-Reference: [P2] [harness/log coverage] — The `## HUD MISMATCHES` section header exists in the report template but the harness emits 0 entries. Confirm the probe is actively comparing all required fields (HP, XP, level, gold, stage label, location, sp1/sp2, 6 skills, 11 polity heat rows, 2 alignment axes) and not silently skipping any of them. A probe that always emits 0 is indistinguishable from a probe that isn't running.

---
*Generated by post-run-analysis.js from playtest-report-20260522-0215-headed.md*