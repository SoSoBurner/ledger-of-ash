# Playtest Fix Plan — 20260526-0142
**Source:** playtest-report-20260526-0113-headed.md
**Generated:** 2026-05-26T08:42:04.930Z

## P0 — Critical (fix before any build)
- Locality Coverage: [P0] Travel system—destinations not being reached**  
- Locality Coverage: [P0] Stage II exploration bottlenecked to 3 core localities**  

## P1 — Important (fix in next session)
- Stage Progression / sp2 Flow: [P1] cosmoria_low_ward — locality visited, 0 sp2 contributed**  
- Economy / Resources: [P1] Economy telemetry missing from report** — The S2_PROBES section is empty. This report contains no data about:
- Locality Coverage: [P1] `cosmoria_low_ward` visited with 0 sp2 contribution**  
- Polish / UX: [P1] Missing screenshot evidence for UX audit**  

## P2 — Nice to have (backlog)
- Stage Progression / sp2 Flow: [P2] S2_PROBES log section empty**  
- Stage Progression / sp2 Flow: [P2] Extreme locality bias — 3 of 28+ Stage II localities visited**  
- Polish / UX: [P2] cosmoria_low_ward — locality without stage-advancement triggers**  
- HUD / G-State Cross-Reference: [P2] HUD probe heartbeat absent from log** — The `## S2 PROBES` section in the report is empty. Per commit `3d3fac53` ("probeHUD cross-references DOM HUD fields vs G-state"), the probe should emit periodic lines comparing DOM-rendered HUD values (HP, XP, level, gold, stage label, location, sp1/sp2, 6 skills, heat rows, alignment) against `G.*` at each pick. Zero probe lines in a 332-pick run means either (a) the probe ran but its output was filtered out of the report excerpt, or (b) it didn't fire. Either way, the "0 HUD mismatches" result is unverified — we can't distinguish "probe ran clean" from "probe was silent."
- HUD / G-State Cross-Reference: [P2] 0 screenshots captured** — The visual leg of HUD verification is missing. We cannot confirm that fields like `gold`, `stage label`, `location name`, and the heat rows (11 polities per CLAUDE.md §Heat System) are rendered correctly and not blank/cut off. Per memory, this is a recurring P2 from the prior session (commit `f490427c` was specifically about diagnosing this), and the fix doesn't appear to have landed in this run.
- HUD / G-State Cross-Reference: [P2] `cosmoria_low_ward` visited but contributed 0 sp2** — Not strictly a HUD issue, but worth a cross-check: if a pick lands in a locality and HUD's sp2 doesn't tick, verify the sp2 readout matched `G.stageProgress[2]` at that pick. With no probe output and no screenshots, we can't tell whether this was a content gap (no sp2-advancing choices authored) or a HUD-not-reflecting-G-state issue.
- UI Duplication / Double-Render Audit: [P2] **harness/screenshot capture** — 0 screenshots captured this run. Cannot visually verify whether any text/stat/button label appears doubled or stacked on-screen. DOM-duplicate logging passed, but the visual layer (stacked CSS, layered overlays, double-paint on transitions) is unverified for this run. Recommend re-running with screenshot capture restored before signing off on UI-dup status for release.
- UI Duplication / Double-Render Audit: [P2] **probe coverage** — `## S2 PROBES` section is empty in the excerpts, so I cannot cross-check whether singleton elements (`#hud-hp`, `.stage-banner`, `.levelup-notice`, `.env-desc`) were actively asserted during the run, or simply not violated. Absence of evidence ≠ evidence of absence here.

---
*Generated by post-run-analysis.js from playtest-report-20260526-0113-headed.md*