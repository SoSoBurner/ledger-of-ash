# Playtest Fix Plan — 20260526-1933
**Source:** playtest-report-20260526-1834-headed.md
**Generated:** 2026-05-27T02:33:18.770Z

## P0 — Critical (fix before any build)
- Locality Coverage: [P0] ithtananalor, cosmoria, aurora, shirshal, soreheim, guildheart, panim — zero sp2 contribution despite being visited**  

## P1 — Important (fix in next session)
- Stage Progression / sp2 Flow: [P1] [Locality Coverage Map] — 7 visited localities contribute zero sp2**
- Economy / Resources: [P1] Playtest harness — economy audit gap** — The harness does not capture transaction logs (gold/supply per pick, shop visits, prices paid, resources consumed). Economy domain cannot be audited without this data. Future headed runs must wire `G.gold`, `G.supply` snapshots and shop transaction logging to validate:
- Locality Coverage: [P1] shelkopolis hyperconcentration — 16 of 21 map travels originate from it**  
- HUD / G-State Cross-Reference: [P1] [harness coverage] — HUD↔G-state cross-reference cannot be verified.** The report's `## HUD MISMATCHES` section is empty, but so are `## VIOLATIONS`, `## S2 PROBES`, `## SUCCESSES`, and `## FAILURES`. No `[hud-integrity]` or `[hud-mismatch]` log entries are present. This indicates the harness did not emit per-pick paired snapshots of (DOM-displayed HUD value, `G` value) for HP, XP, level, gold, stage label, location, sp1/sp2, the six skills (combat/stealth/survival/lore/persuasion/craft), the 11 heat polities, or benevolence/orderAxis. Cannot confirm whether any field drifted from `G` over 481 picks.
- HUD / G-State Cross-Reference: [P1] [harness coverage] — 0 screenshots captured blocks visual HUD audit.** Per `Screenshot List (0 screenshots captured)`. Cannot check for blank fields, cut-off values, mis-rendered heat rows, sp2 progress bar fill, alignment badges (which per CLAUDE.md only appear at ±10 threshold on the character sheet), or `[object Object]` rendering in `G.worldClocks` (a known trap). The 02:13 audit logged "screenshot coverage gap (2 P2)" — that gap is still open here.

## P2 — Nice to have (backlog)
- Locality Coverage: [P2] 23 of 33 Stage I+II localities unvisited**  
- Locality Coverage: [P2] Screenshot capture disabled — 0 screenshots saved**  
- HUD / G-State Cross-Reference: [P2] [indirect signal — consistent]** All 4 families finished with `Final sp2 = 18` and reached `Stage II`. Since the Stage II→antechamber gate is `sp2 ≥ 12` and the engine wrote 18 cleanly for every family, the *underlying* sp2 counter in `G` is being updated and the gate check reads it correctly. This is a weak positive: it confirms `G.stageProgress[2]` is at least internally consistent end-to-end, but says nothing about whether the HUD *display* of sp2 was accurate at intermediate picks.
- HUD / G-State Cross-Reference: [P2] [indirect signal — consistent]** `0 JS errors logged` over 481 picks. Since `G` access in enriched choices is wrapped in `adaptEnrichedChoice`'s try/catch (per CLAUDE.md: "missing keys cause silent TypeErrors swallowed"), a clean JS error count is necessary-but-not-sufficient for HUD correctness — silent G read failures wouldn't surface here. Note this as a known blind spot, not a clean bill of health.
- UI Duplication / Double-Render Audit: [P2] Playtest harness — Screenshot capture disabled this run (0 screenshots across 481 picks, 4 families).** Cannot perform visual duplication review of `#hud-hp`, `.result-text`, `.stage-banner`, `.levelup-notice`, `.env-desc`, or choice-button stacking. Same gap recorded in 20260526-0730 audit (`now.md` 02:12) — recurring harness issue, not a game bug.
- UI Duplication / Double-Render Audit: [P2] Playtest harness — All `## Log Excerpts` sections empty (`VIOLATIONS`, `JS ERRORS`, `DEAD ENDS`, `S2 PROBES`, `SUCCESSES`, `FAILURES`, `HUD MISMATCHES`, `DOM DUPLICATES`).** Either the harness produced zero entries in every category (consistent with the 0/0/0 summary counters) or excerpt extraction is misconfigured. Cannot distinguish "engine clean" from "logging silent" without a positive control.

---
*Generated by post-run-analysis.js from playtest-report-20260526-1834-headed.md*