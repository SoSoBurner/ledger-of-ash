# Playtest Fix Plan — 20260524-1817
**Source:** playtest-report-20260524-1741-headed.md
**Generated:** 2026-05-25T01:17:36.972Z

## P0 — Critical (fix before any build)
- Locality Coverage: [P0] Locality coverage failure — only 3/30+ localities visited in 409 picks**

## P1 — Important (fix in next session)
- Stage Progression / sp2 Flow: [P1] sp2 concentrated in single locality — cosmoria provides 17/18 sp2**
- Stage Progression / sp2 Flow: [P1] Travel system broken — all 14 map travels loop within same locality**
- Stage Progression / sp2 Flow: [P1] sp2 advancement stalled outside cosmoria — coverage gap is massive**
- Locality Coverage: [P1] Map travel system broken — all 14 map travels route back to origin**
- Locality Coverage: [P1] Coverage gaps in populated localities — shelkopolis & fairhaven at 0 sp2**
- Locality Coverage: [P1] Cosmoria over-concentrated — 17/18 total sp2 from 8 visits**
- Tutorial and Onboarding Review: [P1] Map travel system — self-loop pattern** — The travel log shows 14 map travels executed, but almost all are self-loops:
- HUD / G-State Cross-Reference: [P1] [harness instrumentation gap]** — The report's `HUD MISMATCHES` section is empty, but **0 screenshots were captured** and there are no `[hud-integrity]` or `[hud-mismatch]` probe entries in the log excerpts. This is not "no mismatches found" — it is "no HUD verification was performed." Across 409 picks in 4 families, the harness emitted zero HUD cross-reference probes. The empty section is uninformative.
- HUD / G-State Cross-Reference: [P1] [harness instrumentation gap]** — No per-pick G-state snapshot is logged for cross-referencing against the HUD. To diagnose HUD/G drift, the harness needs to (a) read DOM HUD fields (HP, XP, level, gold, stage, location, sp1/sp2, all 6 skills, heat rows, alignment bars) and (b) read the corresponding `G.*` values, then emit `[hud-integrity] OK` or `[hud-mismatch] field=<name> dom=<x> g=<y>` per pick. Currently neither side is captured.

## P2 — Nice to have (backlog)
- HUD / G-State Cross-Reference: [P2] [coverage-related, indirect HUD risk]** — `shelkopolis` and `fairhaven` were visited but contributed **0 sp2**. The sp2 HUD counter therefore stayed flat across many picks in those locations. This is a content/routing issue, not a HUD bug, but it means the sp2 HUD field was never *exercised* with a non-trivial delta in those localities — so even if HUD logging existed, sp2 rendering in shelkopolis/fairhaven is effectively un-tested.
- HUD / G-State Cross-Reference: [P2] [report semantics]** — "Validator warnings (new above baseline): 0" with "Current: 0 | Baseline: 291" suggests the validator now emits **zero** warnings rather than fewer than baseline. Worth confirming validators actually ran — a silent validator passes vacuously. Not a HUD finding per se, but parallel to the screenshot gap: absence of evidence misread as evidence of absence.
- UI Duplication / Double-Render Audit: [P2] harness/instrumentation gap** — 0 screenshots captured this run, so visually-stacked UI (overlapping `.levelup-notice`, doubled `.stage-banner` cosmetic stacking, `#hud-hp` z-index collisions) cannot be confirmed absent. The audit domain explicitly calls out "In screenshots, look for any text, stat value, button label, or UI element that appears visibly doubled or stacked" — that channel was unavailable.
- UI Duplication / Double-Render Audit: [P2] coverage scope** — Only 3 of 30+ localities were visited (shelkopolis, cosmoria, fairhaven). Any locality-specific duplicate-render bug in unvisited locales (cosmouth, soreheim, sheresh, etc.) would not surface in this dataset.

---
*Generated by post-run-analysis.js from playtest-report-20260524-1741-headed.md*