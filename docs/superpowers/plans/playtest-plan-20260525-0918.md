# Playtest Fix Plan — 20260525-0918
**Source:** playtest-report-20260525-0841-headed.md
**Generated:** 2026-05-25T16:18:03.374Z

## P0 — Critical (fix before any build)
- Locality Coverage: [P0] Map travel destinations not being entered**

## P1 — Important (fix in next session)
- Stage Progression / sp2 Flow: [P1] `panim` locality has zero sp2 contribution despite 2 visits**
- Economy / Resources: [P1] [harness instrumentation] — No economy metrics captured in report**
- Locality Coverage: [P1] Panim visited 2 times with 0 sp2 contribution**
- Polish / UX: [P1] Screenshot Capture Not Performed — Visual UX Audit Blocked**
- Branch Drift Audit: [P1] Headed playtest harness — capture screenshots during playtest to enable branch drift audits**
- Tutorial and Onboarding Review: [P1] `panim` locality zero-sp2 contribution** — Visited twice (picks 138, 77) but did not trigger any sp2-advancing choices. This suggests either:
- HUD / G-State Cross-Reference: [P1] [harness/screenshots] — 0 screenshots captured across 400 picks, 4 families.** Report states "Screenshot List (0 screenshots captured)". Without screenshots there is no way to visually verify HP/XP/level/gold/stage/location/sp1/sp2/skills/heat/alignment render correctly in the DOM. The known screenshot-capture failure in headed mode (commit `f490427c`) is still suppressing output — fix must land before next playtest to make HUD audit possible.
- HUD / G-State Cross-Reference: [P1] [harness/s2-probe] — `## S2 PROBES` section in log excerpts is empty.** The `probeHUD` instrumentation added in commit `3d3fac53` cross-references DOM HUD fields against G-state and should emit `[s2-probe]` lines (including economy fields per `e5b1b81b`). Across 400 picks none surfaced into the report excerpt. Either probe is not firing, not being captured by the log collector, or is filtered out of the excerpt aggregation. Without it, "0 [hud-mismatch]" cannot be trusted.

## P2 — Nice to have (backlog)
- Stage Progression / sp2 Flow: [P2] sp2 gain rate varies significantly across families** (0.14–0.29 sp2/pick)
- Stage Progression / sp2 Flow: [P2] Incomplete locality coverage** — Only 5 of 30+ Stage II localities visited; sp2 flow in 27 unvisited localities unverified. Not a blocker for V1.0 if sp2 gate tests pass, but Stage II content expansion should audit coverage across the full map.
- Content / Narrative Quality: [P2] No screenshots captured** — 0 screenshots in report. Screenshot list is empty. Cannot audit choice label text length, result text formatting, or NPC names visually. Recommend re-running with `--capture-screenshots` flag or running a focused headed spec on one family with HUD probes enabled.
- Content / Narrative Quality: [P2] Panim locality — zero sp2 contribution** — Panim visited in 2 runs (picks 138, 50, 71, 77) but never triggered an sp2-advancing choice. Not a narrative quality issue per se, but suggests panim choices either (a) have no sp2-advancement paths, (b) are locked behind unmet conditions, or (c) are gated by prior locality sequences. Recommend: audit `stage2_enriched_choices.js` pool for panim-keyed entries and verify entry conditions.
- Polish / UX: [P2] panim Locality Content Gap**
- HUD / G-State Cross-Reference: [P2] [hud-integrity] — 0 `[hud-integrity] VIOLATION` and 0 `[hud-mismatch]` entries in excerpts.** Within the data the harness did surface, no HUD/G-state discrepancies were flagged across 4 archetypes and 400 picks reaching Stage II (sp2 = 18/21/18/18). This is a positive signal, but conditional on the two P1 telemetry gaps above.
- HUD / G-State Cross-Reference: [P2] [hud/locality-label] — `panim` locality visited twice (picks 138, then ?) with 0 sp2 contribution.** Not a HUD mismatch per se, but worth confirming the location-name field in the HUD did update on entry/exit — `panim` does not appear in the Map Travels Log as either source or destination, suggesting it was reached via non-map-travel (corridor/encounter) and the HUD location field may not have flipped. Verifiable only if `[s2-probe]` lines existed.
- UI Duplication / Double-Render Audit: [P2]** `tests/harness/` (screenshot pipeline) — 0 screenshots captured across 400 picks / 4 families. Headed runs should produce HUD/action-panel snapshots so visual doubling (stacked text, overlapping buttons, doubled stat values) is auditable. Without artifacts, this domain cannot rule out *visual* duplication; it can only rule out *DOM-probe-detected* duplication. Same gap was flagged in the 20260524-1741 audit — recurring blocker for the UI-dup domain.
- UI Duplication / Double-Render Audit: [P2]** `tests/harness/probeHUD` (or DOM-duplicate probe location) — the `## DOM DUPLICATES` section header exists in the report template but produced zero output. Confirm the probe is actually running and emitting `[DUPLICATE]` lines on positive hits, not silently no-op'ing. A truly clean run and a broken probe both look like an empty section; add a single probe-ran heartbeat line (e.g., `DUP_PROBE: scanned N elements`) so future audits can distinguish "clean" from "didn't fire."

---
*Generated by post-run-analysis.js from playtest-report-20260525-0841-headed.md*