# Playtest Fix Plan — 20260605-1658
**Source:** playtest-report-20260605-1506-headed.md
**Generated:** 2026-06-05T23:58:38.895Z

## P0 — Critical (fix before any build)
- Engine / Logic Bugs: [P0]** `ledger-of-ash.html` — Runtime error "Cannot read properties of null (reading 'remove')" logged 12 times across `magic-spellcasting_illusionist_a1` (8 occurrences) and `stealth-precision_scout_c_a1` (2 occurrences). Indicates DOM manipulation code is calling `.remove()` on null element. This is an unhandled null dereference in choice/modal cleanup logic. Despite the error being logged, the illusionist run succeeds; others fail, suggesting error handling is swallowing the failure but leaving corrupted state.
- Engine / Logic Bugs: [P0]** `ledger-of-ash.html` — Stall-timeout failures in 9/15 families (60%): healer, warden, spellthief, alchemist (first run), knight, engineer, bard, scout_c (second run). Game hangs waiting for unknown condition. No logs indicate what the code is stuck on (modal not rendering? async operation hung? choice selector timeout?). Symptoms: runs reach intermediate sp2 values (1–7) then stop making progress. This blocks ~60% of playtests from completion.
- Engine / Logic Bugs: [P0]** `ledger-of-ash.html` — Page-closed failures in 3/15 families (berserker at 100 picks, scout_c at 89 picks, thief at 20 picks) indicate unhandled exceptions crashing the browser tab. No error logs for these runs—crashes are silent at browser level.
- Combat / Balance: [P0] ledger-of-ash.html (DOM cleanup)** — 12 `Cannot read properties of null (reading 'remove')` errors crash illusionist and scout_c families mid-run. This blocks 2 archetype families from completing. Likely a choice/UI removal handler is trying to `.remove()` on a DOM node that was already removed or never existed. Symptom: runs complete 20–100 picks then crash.
- Combat / Balance: [P0] content/stage2_enriched_choices.js or skill system** — Skill usage is nearly zero across the balance matrix. Every archetype shows 0 for stealth, survival, lore, persuasion, craft—only `combat` appears. This means either (1) Stage II content doesn't wire non-combat skills into choices, or (2) the skill-roll system isn't triggering outside combat. For a game with 6+ skills, this is a critical balance gap.
- Locality Coverage: [P0] DOM null.remove() crash blocking magic-spellcasting family**
- Locality Coverage: [P0] Zero sp2 contribution from aurora and glasswake_commune despite successful travel routing**
- Locality Coverage: [P0] Stall-timeout affecting 9/15 families at 60% failure rate**
- Locality Coverage: [P0] DOM null.remove() crash in choice cleanup (magic-spellcasting, stealth-precision)
- Locality Coverage: [P0] aurora + glasswake_commune visited but zero sp2 advancement
- Locality Coverage: [P0] Stall-timeout affecting 60% of families mid-stage-II (choice-load or resolveArrival infinite loop)
- Voice and Register Audit: [P0] Playtest report missing prose excerpts** — The report structure has placeholder sections (VIOLATIONS, FAILURES, S2 PROBES, SUCCESSES, etc.) with **no content** below them. Zero screenshots captured. No result text, NPC dialogue, or narrative content included.
- Voice and Register Audit: [P0] null.remove() crash — 12 instances** — These are the known DOM cleanup null-reference errors flagged in prior sessions. Affects magic-spellcasting and stealth-precision families. Related to enriched-choice error handling swallowing exceptions (memory: *Silent Failure Is the Default*).
- Branch Drift Audit: [P0] Renderer crash — DOM null.remove() pattern**

## P1 — Important (fix in next session)
- Engine / Logic Bugs: [P1]** `ledger-of-ash.html` — Localities `aurora` and `glasswake_commune` visited (5 visits each) but contribute 0 sp2-advancing choices. Either: (1) no choices wired to these localities, (2) choice triggers don't match the arrival conditions, or (3) choices exist but fail to render. Current state: dead-loop locale (you can arrive but no path forward).
- Engine / Logic Bugs: [P1]** Skill roll normalization — Archetype signatures show 0 or very low combat ability triggers across most families (warden, spellthief, engineer, thief have `dominant=none(0)`). Verify that choice roll calls are correctly normalizing old skill keys (`combat` → `might`, `stealth` → `finesse`, etc.) in stage2 enriched choices. If normalization missed a call site, rolls silently fail and fallback to default DC, breaking difficulty scaling.
- Content / Narrative Quality: [P1] Report incomplete — no choice text samples**  
- Combat / Balance: [P1] Stall-Timeout Pattern** — 9 families hit stall-timeout, mostly support-leadership and stealth-precision. Most timeout after 47–63 picks with sp2=1–2, suggesting:
- Combat / Balance: [P1] Aurora & Glasswake Dead Zones** — Both localities visited (5 and 4 times) but contributed 0 sp2 growth. Either:
- Combat / Balance: [P1] Archetype Ability Implementation Gaps** — 6 of 15 archetypes show `dominant=none(0)`, including warden, spellthief, engineer, thief. This means no combat abilities are wired. For a combat-centric game, this looks like incomplete archetype trait/ability wiring.
- Locality Coverage: [P1] Extreme locality clustering — 31/50 map travels routed through shelkopolis hub**
- Locality Coverage: [P1] 21 localities completely unvisited this run**
- Locality Coverage: [P1] page-closed errors on 3 runs with zero sp2 recovery**
- Locality Coverage: [P1] Extreme shelkopolis clustering (27/50 travels) starving other stage-II regions
- Locality Coverage: [P1] 21 localities unvisited; stage 1–2 content mostly dark to players
- Branch Drift Audit: [P1] Coverage gaps — zero sp2 progression**
- Branch Drift Audit: [P1] Stall-timeout endemic — 60% failure rate**
- Tutorial and Onboarding Review: [P1] JS Errors During Early Progression** — 12 `Cannot read properties of null (reading 'remove')` errors logged across first ~100 picks. While not confirmed to be onboarding-specific, these occur early enough (families with 46–100 picks) to potentially corrupt UI guidance text or button states during the first-10-picks window.
- HUD / G-State Cross-Reference: [P1] `magic-spellcasting_illusionist_a1` / `stealth-precision_scout_c_a1` — 12× `Cannot read properties of null (reading 'remove')` during choice-cleanup. If the null node is an HUD-side element (skill row, heat row, alignment bar) being torn down between picks, subsequent `updateHUD()` writes can land on a detached subtree — producing stale or missing fields the screenshot harness would normally catch. With 0 screenshots, this is unverifiable here but is the most plausible HUD-integrity risk in this run.

## P2 — Nice to have (backlog)
- Content / Narrative Quality: [P2] `aurora` locality — zero sp2 advancement**  
- Content / Narrative Quality: [P2] `glasswake_commune` locality — zero sp2 advancement**  
- Combat / Balance: [P2] Thief Early Termination** — Thief (cosmouth) dies at Stage I after only 20 picks, the shortest run. Other single-family runs hit 46–180 picks. Either thief hits an early death condition, or its starting locality/background combo is a progression trap.
- Combat / Balance: [P2] XP Ceiling Enforcement Unclear** — Final sp2 values range 0–22. Successful runs hit 12–22 (reasonable for Stage II, level cap should be 10). Timeout runs hit 1–7, suggesting they stall before level-up. Without actual `G.level` snapshots, I can't confirm level-cap enforcement is working, but the sp2 variance is suspicious.
- Locality Coverage: [P2] Map travel execution is clean but doesn't prevent stalls downstream**
- HUD / G-State Cross-Reference: [P2] sp2 = 0 for `aurora` (5 visits) and `glasswake_commune` (4 visits) — not an HUD bug, but if the sp2 sidebar counter shows progress while G.stageProgress[2] remains 0, that *would* be an HUD/G-state divergence. Worth a targeted screenshot at pick 260 (aurora arrival) and pick 295 (glasswake arrival) in the next run to confirm.

---
*Generated by post-run-analysis.js from playtest-report-20260605-1506-headed.md*