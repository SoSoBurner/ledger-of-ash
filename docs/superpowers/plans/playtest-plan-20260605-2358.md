# Playtest Fix Plan — 20260605-2358
**Source:** playtest-report-20260605-2213-headed.md
**Generated:** 2026-06-06T06:58:25.640Z

## P0 — Critical (fix before any build)
- None detected

## P1 — Important (fix in next session)
- Stage Progression / sp2 Flow: [P1] Three localities with zero sp2 contribution despite multiple visits**
- Stage Progression / sp2 Flow: [P1] Extreme variance in sp2-per-visit rate across localities**
- Content / Narrative Quality: [P1] No screenshot evidence for narrative quality audit** — Report shows 0 screenshots captured, making it impossible to verify choice label length (≤15 words), result text length (60–90 words), or forbidden-word compliance (investigation/meaningful/official/contact). Visual narrative verification was skipped entirely.
- Content / Narrative Quality: [P1] Content coverage gap — Soreheim, Guildheart, Sunspire visited but 0 sp2 contribution** — Three localities were entered but generated zero sp2-advancing choices across all 4 families (521 picks total). This indicates either:
- Content / Narrative Quality: [P1] Violation logs are empty** — All 8 log sections (VIOLATIONS, JS ERRORS, DEAD ENDS, S2 PROBES, SUCCESSES, FAILURES, HUD MISMATCHES, DOM DUPLICATES) show no detail. The report states "0 new warnings above baseline" but provides no supporting evidence—no quoted choice labels, no flagged result text lengths, no canon violations recorded.
- Combat / Balance: [P1] Combat skill rolls critically under-exercised**
- Combat / Balance: [P1] Failure mechanics not tested at scale**
- Combat / Balance: [P1] Heat generation imbalance — stealth archetype dormant**
- Combat / Balance: [P1] XP flow variance — engineer wildly ahead**
- Economy / Resources: [P1] Playtest Harness — Economy instrumentation missing** — The harness does not capture or report any economy/resources metrics (gold balance, supply levels, shop transactions, drain rates, resource sinks). The report shows 0 issues, but this is because **no data is being collected**, not because the economy is working correctly.
- Locality Coverage: [P1] soreheim locality** — visited 4 times, contributed 0 sp2; choice pool lacks sp2-advancing options
- Locality Coverage: [P1] guildheart locality** — visited 2 times, contributed 0 sp2; choice pool lacks sp2-advancing options
- Locality Coverage: [P1] sunspire locality** — visited 3 times, contributed 0 sp2; choice pool lacks sp2-advancing options
- Voice and Register Audit: [P1] Playtest Harness — Prose capture missing; voice audit cannot proceed**
- Tutorial and Onboarding Review: [P1] Tutorial/Onboarding Visual Verification Blocked** — The report shows **(0 screenshots captured)** in a headed playtest run. This prevents visual verification of:
- HUD / G-State Cross-Reference: [P1] [harness/reporter — HUD MISMATCHES bucket]** — The `## HUD MISMATCHES` section is empty across all 521 picks. Per the report's own structure (the bucket exists, so the schema supports it) the harness is *expected* to emit `[hud-mismatch]` lines when HUD fields disagree with `G`. Zero entries combined with **zero screenshots captured** means HUD/G parity was never actually checked this run — the green result is unverified, not verified. CLAUDE.md flags this exact silent-pass class: "A feature is DONE only when verified to produce correct player-facing output."
- HUD / G-State Cross-Reference: [P1] [harness — screenshot capture]** — `Screenshot List (0 screenshots captured)`. With no images, every visual HUD failure mode listed in the task brief (blank field, cut-off text, alignment bar miscoloring, stage label drift, faction-heat row layout) is undetectable. For headed mode in particular this is a regression vs. spec — headed runs exist *for* visual capture.
- HUD / G-State Cross-Reference: [P1] [harness — skill-row telemetry]** — Balance Matrix row for berserker shows `combat=0 stealth=0 survival=0 lore=0 persuasion=0 craft=0` across **214 picks**, and engineer shows the same `0/0/0/0/0/0` across **157 picks**. Either (a) no rolls fired (implausible for 214 picks reaching sp2=18 and Stage II), or (b) the rollD20 telemetry path is broken. Either way, the HUD skill row at `updateHUD()` (~line 10862) cannot be cross-referenced against rolled-skill writes because the writes are not in the log. This blocks any rigorous "skill HUD matches G.skills" check.

## P2 — Nice to have (backlog)
- Stage Progression / sp2 Flow: [P2] Tracking discrepancy — locality sp2 totals vs. family finals**
- HUD / G-State Cross-Reference: [P2] [report — locality coverage gaps as HUD-stability proxy]** — `soreheim`, `guildheart`, `sunspire` show **0 sp2 contribution** despite visits (2–4 each). Not a HUD bug per se, but the *progress bar* in the HUD would render flat across these visits — worth verifying once screenshots return, since a frozen sp2 bar can look like a HUD freeze to playtesters even when the underlying `G.stageProgress[2]` is just unchanged.
- HUD / G-State Cross-Reference: [P2] [data sufficiency]** — No `[hud-integrity] VIOLATION` lines and no `[hud-mismatch]` lines were emitted. Given the harness has these tags wired (per CLAUDE.md's instrumentation contract), the absence is *consistent with* a clean HUD — but not *evidence of* one. Re-run with screenshot + HUD-snapshot instrumentation before drawing release-grade conclusions.
- UI Duplication / Double-Render Audit: [P2] [harness / screenshot capture]** — 0 screenshots captured across 521 picks and 4 families. Visual duplication classes (stacked HUD, double-rendered `.result-text`, doubled `.stage-banner` overlays, ghost `.levelup-notice` from race conditions) cannot be verified from log scan alone. The `## DOM DUPLICATES` and `## HUD MISMATCHES` sections being empty is necessary but not sufficient evidence for "no UI duplication." Recommend headed run with per-pick screenshot capture at minimum on level-up, stage banner, and travel transitions.
- UI Duplication / Double-Render Audit: [P2] [harness / log instrumentation]** — `## DOM DUPLICATES` section exists and is empty, but no positive assertion shows the harness actively counted singleton elements (`document.querySelectorAll('#hud-hp').length`, etc.) per pick. An empty section may mean "no duplicates" or "the check never ran." Recommend adding a per-pick count assertion that logs `[SINGLETON_OK]` so absence becomes evidence-of-absence rather than absence-of-evidence.

---
*Generated by post-run-analysis.js from playtest-report-20260605-2213-headed.md*