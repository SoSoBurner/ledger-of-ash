'use strict';
// Rule 5: Silent worldClock increments — reuses A6 from validate-content.js.

const { checkWorldClockTransparency } = require('../../content/validate-content');

module.exports = { checkWorldClockTransparency };
