# Legacy Reference Archive

This folder preserves superseded or predecessor-labeled reference artifacts that were removed from the active `current_release` surface during V31_3 cleanup hardening. The standalone package uses the release-neutral files in the parent `current_release` directory for active reference use.