# V30_3 World System Overview

V30_3 unifies canon, traversable space, lived locality, runtime state, and table reference into one release surface. The active graph currently spans 15 polity records, 53 major localities, 53 route packets, 71 pressure packets, 489 named NPCs, and 1 runtime effects.

Use the browse order world -> polity -> locality -> route -> pressure -> NPC -> scenario. The world graph files in `07_WORLD_GRAPH/` are the authoritative connectivity layer, while this reference view is the human-readable browse surface.
