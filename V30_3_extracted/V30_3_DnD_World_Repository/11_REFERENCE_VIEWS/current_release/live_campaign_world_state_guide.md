# Live Campaign World State Guide

Read baseline canon first, then pressure packets, then runtime effects, then table-proof scenarios. Promote continuity only when the state change scope registry permits it and the affected world object IDs are explicit.
